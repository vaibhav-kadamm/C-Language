#include <stdio.h>

int main(){
    int principal =101;
    int rate=4; 
    int years=11;
    int simpleinterest=(principal*rate*years);
    printf("the value of simpleinterest is %d",simpleinterest);

    return 0;
}